#include "main.h"
/**
 * _abs - a function that computes the absolute value
 * @n: Used as the variable
 * Return absolute value of n
 */
int _abs(int n)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
	{
		return (c * -1);
	}
}
